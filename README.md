# 🔍 CF Clean IP Scanner

ابزار پیدا کردن IP های تمیز Cloudflare برای Termux (Android ARM64)

<div dir="rtl">

## ویژگی‌ها

- تست سریع و دقیق هزاران IP از رنج‌های Cloudflare
- طراحی و بهینه شده مخصوص Termux  
- تست دو مرحله‌ای: Ping (زیر 500ms) + قابلیت دانلود
- تعداد IP های تست قابل تنظیم (10 تا 1000)
- ذخیره خودکار نتایج در فایل
- بهینه شده برای شرایط فیلترینگ ایران
- مرتب‌سازی بر اساس کمترین Ping

## 📥 نصب

### روش اول: دانلود مستقیم (پیشنهادی برای اکثر کاربران)

**یک دستور ساده در Termux:**

```bash
pkg update && pkg upgrade && pkg install wget unzip && wget https://github.com/4n0nymou3/CF-Clean-IP-Scanner/releases/latest/download/cf-scanner-arm64.zip && unzip cf-scanner-arm64.zip && chmod +x cf-scanner
```

سپس اجرا کنید:

```bash
./cf-scanner
```

**مزایا:**
- ✅ سریع (حدود 30 ثانیه)
- ✅ بدون نیاز به نصب Go
- ✅ فایل آماده و کامپایل شده

---

### روش دوم: Build از سورس (اگر روش اول کار نکرد)

**یک دستور ساده:**

```bash
curl -sL https://raw.githubusercontent.com/4n0nymou3/CF-Clean-IP-Scanner/main/install.sh | bash
```

سپس از هرجا اجرا کنید:

```bash
cf-scanner
```

**مزایا:**
- ✅ 100% سازگار با دستگاه شما
- ✅ Build مستقیم در Termux
- ✅ بدون مشکل compatibility
- ✅ نصب خودکار در PATH

**معایب:**
- زمان بیشتر (حتی تا 10 دقیقه)  
- نیاز به نصب Golang

---

## استفاده

### حالت پیشفرض (500 IP):
```bash
cf-scanner
```

### حالت سفارشی (تعداد دلخواه):
```bash
cf-scanner 100    # تست 100 IP - سریع (3-5 دقیقه)
cf-scanner 250    # تست 250 IP - متوسط (6-10 دقیقه)
cf-scanner 500    # تست 500 IP - پیشفرض (10-15 دقیقه)
cf-scanner 1000   # تست 1000 IP - کامل (20-30 دقیقه)
```

**محدودیت تعداد:** حداقل 10، حداکثر 1000

---

## روند کار ابزار

### مرحله 1️⃣: تست Latency (Ping)

- تست Ping برای **همه IP ها** (حدود 15,000-20,000 عدد)
- فقط IP هایی که **Ping زیر 500ms** دارند حفظ می‌شوند

### مرحله 2️⃣: تست Download  

- از بین IP های با Ping خوب، **تعداد مشخص شده** تست می‌شوند
- چندین URL مختلف برای اطمینان از قابلیت دانلود
- حداقل 256 بایت باید دانلود شود

### نتیجه نهایی

IP ها بر اساس **کمترین Ping** مرتب می‌شوند

---

## مثال خروجی

```
========================================
   CLOUDFLARE CLEAN IP SCANNER
   Find the fastest Cloudflare IPs
========================================

...:..::.::: Designed by: Anonymous :::.::..:...

Version: 1.2.1
Starting Cloudflare Clean IP Scanner...

Optimized for Iran network conditions
2-Stage Test: Latency (ping < 500ms) + Download
Sorted by: Lowest Latency

========================================
      STEP 1: Latency Testing
========================================

Testing latency for 18456 IPs...

[==================================================] 100% (18456/18456) - Found: 1234

Latency test completed: 1234 responsive IPs found

========================================
      STEP 2: Download Speed Test
========================================

[==================================================] 100% (500/500) - Found: 87

Speed test completed: 87 clean IPs found

========================================
        TOP 10 CLEAN IPs FOUND
========================================

Rank   IP Address           Latency      Speed          
------------------------------------------------------
1.     104.16.132.229       89ms         1.23 MB/s      
2.     172.64.153.18        95ms         0.98 MB/s      
3.     104.18.24.24         112ms        1.45 MB/s      
4.     162.159.135.42       127ms        0.87 MB/s      
5.     104.16.88.20         156ms        1.12 MB/s      
...

Results saved to clean_ips.txt successfully!
Total IPs found: 87
```

---

## فایل‌های خروجی

### روش اول (دانلود مستقیم):
- **فایل نتایج:** `clean_ips.txt` (در همان پوشه)

### روش دوم (Build از سورس):  
- **برنامه:** `~/CF-Clean-IP-Scanner/`
- **فایل نتایج:** `~/CF-Clean-IP-Scanner/clean_ips.txt`

**مشاهده نتایج:**
```bash
cat clean_ips.txt
```

یا برای روش دوم:
```bash
cat ~/CF-Clean-IP-Scanner/clean_ips.txt
```

---

## ❓ سوالات متداول

### چرا IP پیدا نمی‌کنه یا تعداد کمی پیدا می‌کنه؟

- در شرایط اینترنت بسیار محدود و فیلترینگ بسیار شدید در ایران، معمولاً تعداد بسیار کمی IP پیدا می‌شود
- تعداد بیشتری تست کنید: `cf-scanner 1000`
- در زمان‌های کم‌ترافیک (شب) امتحان کنید  

### ملاک انتخاب IP ها چیست؟

1. **اول:** Ping زیر 500 میلی‌ثانیه
2. **دوم:** قابلیت دانلود (حداقل 256 بایت)
3. **سوم:** مرتب‌سازی بر اساس کمترین Ping

### چند IP تست می‌کنه؟

- **مرحله 1:** همه IP ها (حدود 15,000-20,000)
- **مرحله 2:** تعداد مشخص شده (پیشفرض 500)

### چقدر طول می‌کشه؟

بستگی به تعداد IP های مرحله 2 دارد:
- 100 IP: 3-5 دقیقه
- 500 IP: 10-15 دقیقه  
- 1000 IP: 20-30 دقیقه

---

## 💡 توصیه‌ها

### برای سرعت بیشتر:
```bash
cf-scanner 50
```

### برای نتیجه بهتر:
```bash
cf-scanner 1000
```

### در شرایط عادی:
```bash
cf-scanner
```
پیشفرض 500 معمولاً کافی است

---

## 🔧 عیب‌یابی

### خطا: Permission denied

```bash
chmod +x cf-scanner
```

### خطا: wget not found

```bash
pkg install wget
```

### خطا: unzip not found

```bash
pkg install unzip
```

### خطا: curl not found

```bash
pkg install curl
```

### خطا: command not found (روش دوم)

مطمئن شوید installer با موفقیت اجرا شده. دوباره امتحان کنید:

```bash
curl -sL https://raw.githubusercontent.com/4n0nymou3/CF-Clean-IP-Scanner/main/install.sh | bash
```

### برنامه کرش می‌کنه

Termux را ریستارت کنید:

```bash
exit
```

سپس Termux را دوباره باز کنید و دوباره امتحان کنید.

### روش اول کار نمی‌کنه

از روش دوم استفاده کنید (Build از سورس) - این روش 100% کار می‌کنه.

---

## 🔄 به‌روزرسانی

### روش اول (دانلود مستقیم):

```bash
rm cf-scanner cf-scanner-arm64.zip clean_ips.txt
wget https://github.com/4n0nymou3/CF-Clean-IP-Scanner/releases/latest/download/cf-scanner-arm64.zip
unzip cf-scanner-arm64.zip
chmod +x cf-scanner
```

### روش دوم (Build از سورس):

```bash
cd ~/CF-Clean-IP-Scanner
git pull
CGO_ENABLED=0 go build -ldflags="-s -w" -o cf-scanner
```

---

## 🗑️ حذف

### روش اول:
```bash
rm cf-scanner cf-scanner-arm64.zip clean_ips.txt
```

### روش دوم:
```bash
rm -rf ~/CF-Clean-IP-Scanner
rm /data/data/com.termux/files/usr/bin/cf-scanner
```

---

## 💡 نکات مهم

- ✅ تست را با VPN خاموش انجام دهید تا IP های واقعی ایران پیدا شوند
- ✅ فایل `clean_ips.txt` را برای استفاده بعدی نگه دارید
- ✅ اگر نتیجه خوب نگرفتید، در زمان دیگری دوباره تست کنید
- ✅ Progress Bar به شما نشان می‌دهد که برنامه در حال کار است
- ✅ حداقل 200-300 MB فضای آزاد داشته باشید
- ✅ اینترنت خود را قطع نکنید تا تست کامل شود

---

## مجوز

این پروژه تحت [مجوز MIT](LICENSE) منتشر شده است - استفاده آزاد

---

## سازنده

طراحی و توسعه توسط: **Anonymous**

📱 ارتباط: [Telegram](https://t.me/BXAMbot)

---

## حمایت از پروژه

اگر این ابزار برای شما مفید بود:

- ⭐ یک Star به repository بدهید
- آن را با دوستانتان به اشتراک بگذارید  

---

**ساخته شده با ❤️ برای کاربران ایرانی**

</div>
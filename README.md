# 🔍 CF Clean IP Scanner

ابزار پیدا کردن IP های تمیز Cloudflare

## ویژگی‌ها

- تست سریع و دقیق هزاران IP
- تست دو مرحله‌ای
- ذخیره خودکار نتایج
- بهینه شده برای شرایط ایران

## نصب

یک دستور ساده در Termux:

```bash
pkg update && pkg upgrade && pkg install wget unzip && wget https://github.com/4n0nymou3/CF-Clean-IP-Scanner/releases/latest/download/cf-scanner-arm64.zip && unzip cf-scanner-arm64.zip && chmod +x cf-scanner
```

## استفاده

### حالت پیشفرض (500 IP):
```bash
./cf-scanner
```

### حالت سفارشی (تعداد دلخواه):
```bash
./cf-scanner 100    # تست 100 IP (سریع‌تر)
./cf-scanner 250    # تست 250 IP
./cf-scanner 500    # تست 500 IP (پیشفرض)
./cf-scanner 1000   # تست 1000 IP (بیشترین)
```

**محدودیت**: حداقل 10، حداکثر 1000

## روند کار

### مرحله 1: تست Latency (Ping)
ابتدا تمام IP ها تست می‌شوند و فقط IP هایی که **ping زیر 500ms** دارند حفظ می‌شوند.

### مرحله 2: تست دانلود
از بین IP های با ping خوب، تست دانلود انجام می‌شود تا مطمئن شویم IP واقعاً **قابلیت دانلود** دارد.

### نتیجه
IP ها بر اساس **کمترین ping** مرتب می‌شوند.

## مثال خروجی

```
========================================
        TOP 10 CLEAN IPs FOUND
========================================

Rank   IP Address           Latency      Speed          
------------------------------------------------------
1.     104.16.132.229       89ms         1.23 MB/s      
2.     172.64.153.18        95ms         0.98 MB/s      
3.     104.18.24.24         112ms        1.45 MB/s      
4.     162.159.135.42       127ms        0.87 MB/s      
5.     104.16.88.20         156ms        1.12 MB/s      
...
```

نتایج در فایل `clean_ips.txt` ذخیره می‌شوند.

### ملاک انتخاب IP ها چیست؟

1. **اول**: Ping زیر 500ms
2. **دوم**: قابلیت دانلود (حداقل 256 بایت)
3. **سورت**: بر اساس کمترین ping

### چند IP تست می‌کنه؟

حدود 15000-20000 IP از رنج‌های Cloudflare

## عیب‌یابی

### خطا: Permission denied

```bash
chmod +x cf-scanner
```

### خطا: wget not found

```bash
pkg install wget
```

### خطا: unzip not found

```bash
pkg install unzip
```

### برنامه کرش می‌کنه

Termux را ریستارت کنید:

```bash
exit
```

سپس Termux را دوباره باز کنید.

### گیر کرده در مرحله 2

صبور باشید! تست دانلود زمان‌بر است. Progress bar نشان می‌دهد چقدر پیشرفت کرده.

## به‌روزرسانی

نسخه جدید منتشر شد؟

```bash
rm cf-scanner cf-scanner-arm64.zip
wget https://github.com/4n0nymou3/CF-Clean-IP-Scanner/releases/latest/download/cf-scanner-arm64.zip
unzip cf-scanner-arm64.zip
chmod +x cf-scanner
```

## حذف

```bash
rm cf-scanner cf-scanner-arm64.zip clean_ips.txt
```

## لایسنس

MIT License - استفاده آزاد

## نکات

- تست را VPN خاموش انجام دهید
- فایل `clean_ips.txt` را برای استفاده بعدی نگه دارید
- اگر نتیجه خوب نگرفتید، بعداً دوباره امتحان کنید
- Progress bar به شما نشان می‌دهد فرآیند در حال انجام است

---

## مجوز

این پروژه تحت مجوز MIT منتشر شده است. برای اطلاعات بیشتر فایل [LICENSE](LICENSE) را مشاهده کنید.

## سازنده

طراحی و توسعه توسط: [Anonymous](https://t.me/BXAMbot)

---

⭐ اگر این پروژه برای شما مفید بود، یک ستاره به آن بدهید!
name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., 1.3.0)'
        required: true
        type: string
      prerelease:
        description: 'Mark as pre-release'
        required: false
        type: choice
        options:
          - 'false'
          - 'true'
        default: 'false'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23.x'
    
    - name: Get dependencies
      run: |
        go mod download
        go mod tidy
    
    - name: Build for Android ARM64
      run: |
        CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o cf-scanner
    
    - name: Verify build
      run: |
        file cf-scanner
        ls -lh cf-scanner
    
    - name: Create zip file
      run: |
        zip cf-scanner-arm64.zip cf-scanner
    
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ github.event.inputs.version }}
        release_name: v${{ github.event.inputs.version }}
        body: |
          ## CF Clean IP Scanner v${{ github.event.inputs.version }}
          
          **Cloudflare Clean IP Scanner for Termux (Android ARM64)**
          
          ### Installation Method 1: Direct Download (Recommended)
          
          ```bash
          pkg update && pkg upgrade && pkg install wget unzip
          wget https://github.com/${{ github.repository }}/releases/download/v${{ github.event.inputs.version }}/cf-scanner-arm64.zip
          unzip cf-scanner-arm64.zip
          chmod +x cf-scanner
          ./cf-scanner
          ```
          
          ### Installation Method 2: Build from Source (If method 1 fails)
          
          ```bash
          curl -sL https://raw.githubusercontent.com/${{ github.repository }}/main/install.sh | bash
          ```
          
          ### Usage
          
          ```bash
          cf-scanner
          ```
          
          The tool works in two steps:
          - **Step 1**: Automatically scans all IPs for latency (< 1000ms)
          - **Step 2**: Asks you how many of the responsive IPs to speed test (you choose the number)
          
          You can press **Ctrl+C** at any time to stop the scan and see all clean IPs found so far.
          
          At the end of every scan, the tool shows:
          - Total scan duration
          - Estimated data used
          
          ### Requirements
          
          - Termux (latest version)
          - Android ARM64 device
          - Internet connection
          
          ---
          
          Made with ❤️ for Iranian users
        draft: false
        prerelease: ${{ github.event.inputs.prerelease == 'true' }}
    
    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./cf-scanner-arm64.zip
        asset_name: cf-scanner-arm64.zip
        asset_content_type: application/zip